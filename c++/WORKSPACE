workspace(name = "capnp-cpp")

#
# NOTE: If you're looking at this file to determine how to install Cap'n'Proto in your own
#       Bazel project, please see the project README; initialization steps here differe from
#       what they should be in your project.
#

# Import repositories
load("//defs:repositories.bzl", "capnp_repositories")

# Only Brotli is actually imported here, because it isn't yet available on Bazel Registry.
capnp_repositories(
    omit_skylib = True,
    omit_boringssl = True,
    omit_zlib = True,
)
